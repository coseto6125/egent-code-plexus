# Homebrew tap formula template for coseto6125/homebrew-tap.
#
# Release automation should replace:
#   @VERSION@
#   @SHA256_ARM64_DARWIN@
#   @SHA256_X86_64_DARWIN@
#
# Expected GitHub Release assets:
#   gnx-v@VERSION@-aarch64-apple-darwin.tar.gz
#   gnx-v@VERSION@-x86_64-apple-darwin.tar.gz

class GraphNexus < Formula
  desc "Code intelligence graph CLI for LLM agents"
  homepage "https://github.com/coseto6125/graph-nexus"
  license "MIT OR Apache-2.0"
  version "@VERSION@"

  on_macos do
    if Hardware::CPU.arm?
      url "https://github.com/coseto6125/graph-nexus/releases/download/v#{version}/gnx-v#{version}-aarch64-apple-darwin.tar.gz"
      sha256 "@SHA256_ARM64_DARWIN@"
    else
      url "https://github.com/coseto6125/graph-nexus/releases/download/v#{version}/gnx-v#{version}-x86_64-apple-darwin.tar.gz"
      sha256 "@SHA256_X86_64_DARWIN@"
    end
  end

  def install
    bin.install "gnx"
  end

  test do
    system "#{bin}/gnx", "--version"
  end
end
