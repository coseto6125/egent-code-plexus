# cgn routes

List HTTP routes and their handler chains.

## Usage
```bash
cgn routes [<PATH_PATTERN>] [--repo <PATH>]
```

## Best For
- Auditing the API surface of a service.
- Finding the handler for a specific endpoint.
- Seeing the upstream callers of a route.
