name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (build only, no publish/release)'
        required: false
        default: 'false'

permissions:
  contents: write       # GitHub Release 上傳
  id-token: write       # SLSA OIDC token
  attestations: write   # actions/attest-build-provenance

# 同一 tag/手動觸發避免重複跑，但不取消已 publish 中的（保證 release atomicity）
concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  CARGO_NET_RETRY: 10
  RUSTUP_MAX_RETRIES: 10
  BIN_NAME: gnx

jobs:
  # ==================== Multi-platform binary builds ====================
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            archive: tar.gz
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-24.04-arm
            archive: tar.gz
          - target: x86_64-apple-darwin
            os: macos-13
            archive: tar.gz
          - target: aarch64-apple-darwin
            os: macos-latest
            archive: tar.gz
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            archive: zip

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          # 每個 target 獨立 cache（不同 ABI，無法共用 target/）
          # 與 ci.yml 的 cache 分離，因為 release 是 --release profile
          shared-key: release-${{ matrix.target }}

      - name: Build release binary
        run: cargo build --release --bin ${{ env.BIN_NAME }} --target ${{ matrix.target }}

      - name: Package (tar.gz)
        if: matrix.archive == 'tar.gz'
        shell: bash
        run: |
          name="${BIN_NAME}-${GITHUB_REF_NAME:-dev}-${{ matrix.target }}"
          mkdir -p "dist/${name}"
          cp "target/${{ matrix.target }}/release/${BIN_NAME}" "dist/${name}/"
          cp README.md LICENSE NOTICE "dist/${name}/" 2>/dev/null || true
          tar -C dist -czf "dist/${name}.tar.gz" "${name}"
          ( cd dist && shasum -a 256 "${name}.tar.gz" > "${name}.tar.gz.sha256" )

      - name: Package (zip)
        if: matrix.archive == 'zip'
        shell: pwsh
        run: |
          $name = "${env:BIN_NAME}-${env:GITHUB_REF_NAME}-${{ matrix.target }}"
          if (-not $env:GITHUB_REF_NAME) { $name = "${env:BIN_NAME}-dev-${{ matrix.target }}" }
          New-Item -ItemType Directory -Force -Path "dist/$name" | Out-Null
          Copy-Item "target/${{ matrix.target }}/release/${env:BIN_NAME}.exe" "dist/$name/"
          foreach ($f in @('README.md','LICENSE','NOTICE')) {
            if (Test-Path $f) { Copy-Item $f "dist/$name/" }
          }
          Compress-Archive -Path "dist/$name" -DestinationPath "dist/$name.zip"
          $hash = (Get-FileHash "dist/$name.zip" -Algorithm SHA256).Hash.ToLower()
          "$hash  $name.zip" | Out-File -Encoding ASCII "dist/$name.zip.sha256"

      - name: Generate SLSA build provenance
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: 'dist/*.tar.gz dist/*.zip'

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.BIN_NAME }}-${{ matrix.target }}
          path: |
            dist/*.tar.gz
            dist/*.zip
            dist/*.sha256
          if-no-files-found: error

  # ==================== GitHub Release ====================
  github-release:
    name: GitHub Release
    needs: [build]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/') && inputs.dry_run != 'true'

    steps:
      - name: Checkout (for install scripts)
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Stage install scripts into dist
        run: |
          cp scripts/install.sh  dist/install.sh
          cp scripts/install.ps1 dist/install.ps1
          ls -la dist/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/*.tar.gz
            dist/*.zip
            dist/*.sha256
            dist/install.sh
            dist/install.ps1
          draft: false
          prerelease: false
          generate_release_notes: true
          body: |
            ## Install

            ### macOS Homebrew
            ```
            brew tap coseto6125/tap
            brew install graph-nexus
            ```

            ### One-shot installer scripts (Linux / macOS)
            ```
            curl -sSfL https://github.com/coseto6125/graph-nexus/releases/download/${{ github.ref_name }}/install.sh | sh
            ```

            ### One-shot installer (Windows PowerShell)
            ```
            irm https://github.com/coseto6125/graph-nexus/releases/download/${{ github.ref_name }}/install.ps1 | iex
            ```

            ### From git tag (source build)
            ```
            cargo install --git https://github.com/coseto6125/graph-nexus --tag ${{ github.ref_name }} --bin gnx
            ```

            ### Manual download
            Pick a `.tar.gz` / `.zip` matching your platform from Assets below.
            Each has a `.sha256` for integrity. Verify build provenance with:
            ```
            gh attestation verify ./gnx --owner coseto6125
            ```

  # ==================== Homebrew Tap ====================
  # Requires:
  #   1. GitHub repo: coseto6125/homebrew-tap
  #   2. Secret HOMEBREW_TAP_TOKEN with write access to that repo
  homebrew-tap:
    name: Update Homebrew tap
    needs: [github-release]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/') && inputs.dry_run != 'true'
    env:
      HOMEBREW_TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download release artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Generate Homebrew formula
        id: formula
        shell: bash
        run: |
          if [ -z "${HOMEBREW_TAP_TOKEN}" ]; then
            echo "::notice::HOMEBREW_TAP_TOKEN is not set; skipping Homebrew tap update."
            echo "skip=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          version="${GITHUB_REF_NAME#v}"
          arm_sha_file="dist/gnx-${GITHUB_REF_NAME}-aarch64-apple-darwin.tar.gz.sha256"
          x86_sha_file="dist/gnx-${GITHUB_REF_NAME}-x86_64-apple-darwin.tar.gz.sha256"

          if [ ! -f "$arm_sha_file" ] || [ ! -f "$x86_sha_file" ]; then
            echo "::error::Missing macOS sha256 files in dist/."
            ls -la dist
            exit 1
          fi

          arm_sha="$(awk '{print $1}' "$arm_sha_file")"
          x86_sha="$(awk '{print $1}' "$x86_sha_file")"

          mkdir -p generated/Formula
          sed \
            -e "s/@VERSION@/${version}/g" \
            -e "s/@SHA256_ARM64_DARWIN@/${arm_sha}/g" \
            -e "s/@SHA256_X86_64_DARWIN@/${x86_sha}/g" \
            packaging/homebrew/graph-nexus.rb.template \
            > generated/Formula/graph-nexus.rb

      - name: Push formula to tap
        if: steps.formula.outputs.skip != 'true'
        shell: bash
        run: |
          git clone "https://x-access-token:${HOMEBREW_TAP_TOKEN}@github.com/coseto6125/homebrew-tap.git" tap
          mkdir -p tap/Formula
          cp generated/Formula/graph-nexus.rb tap/Formula/graph-nexus.rb

          cd tap
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          if git diff --quiet -- Formula/graph-nexus.rb; then
            echo "Homebrew formula is already up to date."
            exit 0
          fi

          git add Formula/graph-nexus.rb
          git commit -m "graph-nexus ${GITHUB_REF_NAME}"
          git push

  # ==================== Publish to crates.io ====================
  # 狀態：
  #   graph-nexus-core ── publish-ready (cargo publish --dry-run 通過)
  #   graph-nexus-analyzer ── 阻塞於 git/vendor deps 沒 version：
  #     - tree-sitter-crystal (git dep) 需給 version 或改 crates.io 版
  #     - tree-sitter-kotlin (git dep) 同上
  #     - tree-sitter-move / tree-sitter-swift (vendored path dep) 需先發到
  #       crates.io 或標 publish = false 在 vendor side
  #   graph-nexus ── 需 graph-nexus-core + graph-nexus-analyzer 先 publish
  # Token 設定：repo Settings → Secrets → CARGO_REGISTRY_TOKEN
  # 解掉上述阻塞後，把 if: false 改為:
  #   if: startsWith(github.ref, 'refs/tags/') && inputs.dry_run != 'true'
  publish-crates:
    name: Publish to crates.io
    needs: [build]
    runs-on: ubuntu-latest
    if: false

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Publish graph-nexus-core
        run: cargo publish -p graph-nexus-core --token ${{ secrets.CARGO_REGISTRY_TOKEN }}

      - name: Publish graph-nexus-analyzer
        run: cargo publish -p graph-nexus-analyzer --token ${{ secrets.CARGO_REGISTRY_TOKEN }}

      - name: Publish graph-nexus (cli)
        run: cargo publish -p graph-nexus --token ${{ secrets.CARGO_REGISTRY_TOKEN }}
